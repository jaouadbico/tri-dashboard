# Copy to .env for local runs (never commit the real .env).
# Shared secret the Supabase Edge Function must send as X-Internal-Secret.
# Generate one with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
INTERNAL_API_SECRET=
